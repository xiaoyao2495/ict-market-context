# Archived legacy MSS research

This directory preserves the former "latest unconsumed swing + break-quality"
MSS detector and its diagnostic/backtest consumers. It is outside production and
the active test graph. Production opportunities consume only close-confirmed
`STRUCTURAL_MSS` events from `structure/structuralProvenance5m.js`.
