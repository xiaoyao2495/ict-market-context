{
  "schema": "EQClusterV3",
  "schemaVersion": 3,
  "minimumMembers": 2,
  "identityFields": {
    "id": "Stable current-compatible id: symbol + ':' + type + ':' + formationAnchor.sourceOpenTime",
    "symbol": "string",
    "timeframe": "string",
    "type": "EQH | EQL",
    "side": "BSL for EQH | SSL for EQL"
  },
  "immutableFormationFields": {
    "formationAnchor": {
      "canonicalSwingId": "string",
      "price": "number",
      "sourceOpenTime": "number",
      "confirmedAt": "number"
    },
    "createdAt": "First time the cluster reaches two qualified members",
    "confirmedAt": "Same value as createdAt; never changes after append",
    "initialMemberIds": "Exactly the two canonical Swing ids that created the cluster"
  },
  "appendOnlyMemberLedger": {
    "members": [
      {
        "canonicalSwingId": "string",
        "price": "number",
        "sourceOpenTime": "number",
        "confirmedAt": "Swing confirmation time",
        "memberAddedAt": "Time membership first became visible in this cluster",
        "membershipReason": "INITIAL_FORMATION | QUALIFIED_APPEND",
        "anchorPairClassification": "VALID_EQ"
      }
    ],
    "uniquenessKey": "clusterId + canonicalSwingId",
    "ordering": "memberAddedAt, confirmedAt, sourceOpenTime, canonicalSwingId"
  },
  "projectedFields": {
    "referencePrice": "Arithmetic mean of member prices visible as-of evaluationTime",
    "memberCount": "Count of members visible as-of evaluationTime; factual metadata only",
    "lastMemberConfirmedAt": "Maximum confirmedAt among visible members",
    "lastMemberAddedAt": "Maximum memberAddedAt among visible members",
    "state": "Existing production lifecycle state projected as-of evaluationTime"
  },
  "invariants": {
    "CLUSTER_MIN_MEMBERS": 2,
    "CLUSTER_ID_STABLE_AFTER_MEMBER_APPEND": true,
    "CLUSTER_ORIGINAL_CONFIRMED_AT_IMMUTABLE": true,
    "REFERENCE_PRICE_FORMULA_CHANGED": false,
    "MEMBER_COUNT_IS_QUALITY_RANK": false,
    "CLUSTER_MERGE_V3": "NOT_SUPPORTED",
    "CLUSTER_SPLIT_V3": "NOT_SUPPORTED"
  }
}
